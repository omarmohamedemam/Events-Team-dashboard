export default function TrainingPage() {
  return (
    <section className="card">
      <div className="empty-state">
        <strong>Training tracker is next.</strong>
        <p>Manual event workflow is in place first; add sessions and training attendance after live event data is stable.</p>
      </div>
    </section>
  );
}
